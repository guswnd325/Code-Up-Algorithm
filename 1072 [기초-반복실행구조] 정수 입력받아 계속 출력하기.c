#include <stdio.h> // 헤더파일 불러오기 


int main(void) { // 메인 함수 실행 
	int i, n;
	scanf("%d", &n); 
	for (i = 0; i < n; i++) {
		int x;
		scanf("%d", &x);
		printf("%d\n", x);
	}
		return 0; // 0 반환 d
	}


	


