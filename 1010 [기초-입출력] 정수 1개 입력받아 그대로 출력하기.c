#include <stdio.h> // 헤더파일 불러오기 

int main(void) { // 메인 함수 실행 
 
	int number; // 정수형 변수 number 선언. 
	scanf("%d", &number); // scanf함수로 number변수에 할당함. 
	printf("%d", number); // number 변수값 출력. %d는 정수를 출력할때 사용함. 
	return 0; // 0 반환 
}
