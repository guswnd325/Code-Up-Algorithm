#include <stdio.h> // 헤더파일 불러오기 

int main(void) { // 메인 함수 실행 
 
    char text; // 문자형 변수 text 선언  
    scanf("%c", &text); // scanf함수를 통한 입력받기 -> text변수에 할당 
    printf("%c", text); // 입력받은 변수를 출력함
    return 0; // 0 반환 
 
}


