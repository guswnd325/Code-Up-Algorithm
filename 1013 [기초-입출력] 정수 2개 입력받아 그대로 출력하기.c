#include <stdio.h> // 헤더파일 불러오기 

int main(void) { // 메인 함수 실행 
 
    int NUM1, NUM2; // 정수형 변수 text 선언  
    scanf("%d %d", &NUM1, &NUM2); // scanf함수를 통한 입력받기 -> NUM1, NUM2변수에 각각 할당 
    printf("%d %d", NUM1, NUM2); // 입력받은 변수를 출력함
    return 0; // 0 반환 
 
}


