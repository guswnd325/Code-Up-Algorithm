#include <stdio.h> // 헤더파일 불러오기 

int main(void) { // 메인 함수 실행 
	long long int a; // long long int 변수 a 선언 
	scanf("%lld", &a); // long long int = %lld , 입력 받음.
	printf("%lld", a); // %lld (long long int(d)) 값에 a를 넣어 출력 
	
    return 0; // 0 반환 
 
}



