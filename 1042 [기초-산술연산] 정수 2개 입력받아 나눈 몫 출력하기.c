#include <stdio.h> // 헤더파일 불러오기 

int main(void) { // 메인 함수 실행 
	int a,b; // 정수형 변수 a,b 선언. 
	
	scanf("%d %d", &a, &b); // a,b 변수의 값을 입력받고 할당함. 
	
	printf("%d", (a / b)); // a를 b로 나눈 몫을 출력함. 
	
    return 0; // 0 반환 
 
}




